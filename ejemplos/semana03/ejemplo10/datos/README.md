# Datos del Ejemplo 10

El ejemplo reutiliza `datos/semana03/cargar_datos_semana03.js`.
`zonas_riesgo_fuente` contiene tres anillos de vértices; la demostración
reconstruye `zonas_riesgo` con un polígono GeoJSON por documento.

Las zonas son sintéticas y no representan cartografía oficial.
